%h1 Listing cpus

%table
  %thead
    %tr
      %th Pc
      %th Chipmake
      %th Chipmodel
      %th Aftermarketmake
      %th
      %th
      %th

  %tbody
    - @cpus.each do |cpu|
      %tr
        %td= cpu.Pc
        %td= cpu.ChipMake
        %td= cpu.ChipModel
        %td= cpu.AfterMarketMake
        %td= link_to 'Show', cpu
        %td= link_to 'Edit', edit_cpu_path(cpu)
        %td= link_to 'Destroy', cpu, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Cpu', new_cpu_path
